# iOS App Placeholder

This folder is reserved for the future iOS version of the AI Blueprint Scan App.

Recommended implementation options:
- React Native
- Expo
- SwiftUI

Planned integration approach:
- Use the same backend API that the current web application uses
- Reuse shared authentication, project, takeoff, estimate, and export workflows
- Keep mobile-specific UI and device features isolated in this folder

Current status:
- No iOS application code has been moved here yet
- The existing mobile companion app under `apps/mobile` remains separate from this future dedicated iOS track
