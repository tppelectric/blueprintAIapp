# Utility Rules Module Scaffold

This folder defines the target production architecture location for utility-rule logic.

Current runtime source of truth:
- `packages/shared/src/modules/utility-rules/*`

These files re-export shared implementations while we migrate incrementally without breaking existing routes.

